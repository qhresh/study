<?php
// 随机生成两个数组A和B，A中有10个元素，B中有100个元素，获取他们的交集
$arrA = array();
$arrB = array();

for($i = 0; $i < 100; $i++)
{
	if($i % 10 == 0)
	{
		$arrA[] = rand(1, 100);
	}
	$arrB[] = rand(1, 100);
}
print_r($arrA);
print_r($arrB);
print_r(array_intersect($arrA, $arrB));						// 使用php内置函数array_intersect
