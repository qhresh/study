<?php
$xml = simplexml_load_file("demo.xml");

if(isset($xml->operation_request->msgbody->rechabkcardno))
{
	echo $xml->operation_request->msgbody->rechabkcardno . PHP_EOL;
}
else
{
	echo "can not find the node!\n";
}

echo $xml->operation_request->msgbody->rechabkcardno;
